# build output
dist/
# generated types
.astro/

# Vite cache
node_modules/.vite/

# dependencies
node_modules/

# -----------------------------------------------------------------------------
# Data and caches (generated at runtime or by scripts - do not commit)
# -----------------------------------------------------------------------------
data/systemCache.json
data/eddnMarketCache.json
data/edsmMarketData.json
data/rares.json
data/rareSystemsCache.json
data/curatedLegality.json
data/curatedPrices.json
# Ignore any other JSON in data/ (keep only data/.gitkeep in repo)
data/*.json

# -----------------------------------------------------------------------------
# Logs (never commit)
# -----------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# -----------------------------------------------------------------------------
# Environment and secrets (never commit)
# -----------------------------------------------------------------------------
.env
.env.*
.env.local
.env.production
.config.json

# -----------------------------------------------------------------------------
# OS and IDE (local machine only)
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini
.idea/
*.swp
*.swo
*~

# User-specific VS Code (optional: remove if you want to share .vscode)
.vscode/settings.json
.vscode/*.local
